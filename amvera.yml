---
meta:
  environment: jvm
  toolchain:
    name: maven
    version: 17
build:
  artifacts:
    target/*.jar: /
run:
  jarName: Search_Engine-1.0-SNAPSHOT.jar
  persistenceMount: /data
  containerPort: 8080
